<!--ヘッダー-->
<%= render 'layouts/header' %>
  <div class="container-fluid">
    <div class="row mx-5 mt-5">
      <!--サイド(左)-->
      <%= render "layouts/side", post_categories: @post_categories, question_categories: @question_categories, hashtags: @hashtags %>
        <!--一覧(右)-->
        <div class="col-md-10 pl-1">

          <div class='row mx-5 justify-content-start'>
            <%= link_to post_images_path, class: 'nav-link text-dark' do %>
              <p class="font-weight-bold">投稿を見る</p>
            <% end %>
            <%= link_to questions_path, class: 'nav-link text-dark' do %>
              <p>質問を見る</p>
            <% end %>
          </div>
          <div class='row mx-5 justify-content-start list-unstyled'>
            <li>
            <div class="border border-secondary px-2 py-2">
              Category
            </div>
            </li>
            <% @post_categories.each do |category| %>
            <li class="pb-2 px-4">
              <%= link_to category.post_category,post_images_path(post_category: category.id), class: 'text-dark' %>
           </li>
            <% end %>
          </div>

          <% @post_images.each do |image| %>
            <div class="mx-3 my-4">
              <div class="card border-success shadow p-3 mx-3">
               <div class= "mx-auto">


                <div class="mt-1 pt-3 pl-3">
                  <!--アイコン-->
                  <%= link_to user_path(image.user),class:"text-dark" do %>
                    <%= image_tag image.user.get_profile_image(130,130),class: "icon rounded-circle" %>
                    <%= image.user.name %>
                  <% end %>
                </div>
                　<div class= "index-image col-md-11">
                    <!--写真-->
                    <%= image_tag image.get_image(700,520)%>
                  </div>
                    <p class="font-weight-bold card-title pl-3 pt-2 col-md-11">
                    <!--タイトル-->
                      <%= link_to post_image_path(image.id), class:"text-dark" do %>
                        <%= image.title %>
                      <% end %>
                    </p>
                    <p class="card-title pl-3 pt-2 col-md-11">
                    <!--内容,ハッシュタグリンク-->
                      <%= render_with_hashtags(image.body) %><br>
                      <%= image.created_at.strftime('%Y/%m/%d') %>
                    </p>

                  <h2 class="text-right favorite-btn_<%= image.id %>">
                    <%= render "public/favorites/favorite-btn",post_image: image %>
                  </h2>

                </div>
                </div>
              </div>
          <% end %>
        </div>
      </div>

    </div>
  </div>
<!--フッター-->
<%= render 'layouts/footer' %>