<% unless current_user.name == "guestuser" %>
  <% if question.question_favorited_by?(current_user) %>
    <%= link_to question_question_favorites_path(question), method: :delete, remote: true do %>
      ♥<%= question.question_favorites.count %>
    <% end %>
  <% else %>
    <%= link_to question_question_favorites_path(question), method: :post, remote: true do %>
     ♡<%= question.question_favorites.count %>
    <% end %>
  </p>
  <% end %>
<% end %>